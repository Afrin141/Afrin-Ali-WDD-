// Function to handle order tracking
function trackOrder(orderNumber) {
    alert("Tracking for Order Number: " + orderNumber);
    // Implement tracking logic here (e.g., redirect to a tracking page or fetch data)
 }
 
 // Example of dynamically adding orders (optional enhancement)
 document.addEventListener('DOMContentLoaded', () => {
    const orderList = document.getElementById('order-list');
    // You can dynamically add rows here if needed
 });
 